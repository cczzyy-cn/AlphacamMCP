# 开料小板件防松动算法方案 —— HBT（Hold-Budget + Taper-to-Zero）

> 日期：2026-09-13
> 前置：`开料小板件吸附与斜下刀算法分析.md`（机理、缺陷、现有实现对比）
> 本文是**设计方案**，不是实测结论。已实测/有代码依据的部分标 ✅，属推断需标定的部分标 ⚠️。

---

## 0. 一句话

**把「真空保持力」当成一个会被加工过程消耗的资源来调度，并让每一条刀路的吃刀负载在它造成
不可逆状态变化（轮廓闭合 = 板件切离）的那一刻归零。**

⛔ 关键判断：现有 `小条先切` / `modRamp` 都是在做**几何启发式**（找邻件、找角、找边），
而它们唯一真正有效的机制 —— 「末端归零」（分析报告 §1.3 ✅）—— 其实是**负载时序**的性质。
所以与其继续调 `SmallWidth=260` / `SloopDist=20` / `BackDist=40` 这些阈值，
不如直接以**负载与保持力**为建模对象。这是一次建模对象的更换，不是参数微调。

---

## 1. 三条物理事实（方案的立足点）

### 1.1 释放瞬间的负载可以做到几何归零 ✅

斜坡锚定在**轮廓末端**时，主切段要切的是斜坡楔形的**补集**，
负载在最后 `sloopDist` 上线性降到 0，而轮廓恰在此处闭合（分析报告 §1.3）。
**与坡角无关，是纯几何性质。**

→ 推论：这个性质应该被**推广到每一条分离刀**，而不只是小条。
`小条先切` 的大板通道 `RoughFinishBigPanel` 完全没有斜坡（§1.5a ✅），
即**它对自己最重的那些件根本没做这件事**。

### 1.2 「切透」会摧毁该件底下的真空密封，而「留皮」不会 ⚠️(机理清晰，量级需标定)

- 件未被切透时：底部与垫板之间仍是密封的，**吸力全额保留**；
  一圈不到底的皮同时封住了空气从切缝钻到底下的路径。
- 件被切透时：切缝成为泄漏通道，该件底下压力迅速回升，吸力塌到只剩很小一部分
  （分析报告 §4.1 估算：约 20%，即吸力掉到 1/5）。
- 因此留皮不是「用机械连接代替真空」，而是**同时保住真空 + 增加机械约束**，
  两件事一起发生。这是它比斜下刀高一个量级的原因。

### 1.3 板件「松动」的可逆性

件的位移一旦发生就不可逆（刀具会撞上去）。所以调度目标不是「平均风险最小」，
而是**不允许任何一个时刻出现「已释放的件 + 满负载刀路」的组合**。

---

## 2. 方案主体

### 2.1 核心结构：把「分离刀」全部推迟到最后一趟

对每条闭合刀路做一次分类：

```
分离刀 (releasing cut)  = 该刀路完成后，会有一件从板材上分离的那些刀路
非分离刀                = 其余（槽、内轮廓、同一件被拆成多段的刀路）
```

然后：

```
pass 1 —— 全部件切到 T - s        (s = 0.8mm 左右)
          期间【没有任何一件被切离】→ 整板密封完整、无自由件
          顺序：只需优化空行程（与保持力无关！）
pass 2 —— 全部件切透 (T + 0.3mm)
          负载只有满深的 s/T ≈ 4%  ✅(几何)
          每条刀路末端仍归零        ✅(§1.1)
          顺序：按 §2.3 的 HBT 评分升序
          进给：按 §2.4 力约束整形
```

**这一步把整个问题降级了**：pass 1 的排序不再影响保持力，
于是原来那个「小条先切还是大板先切、按什么优先级」的难题**消失了**，
只剩空行程优化。这是本方案相对现有实现最大的结构性收益。

### 2.2 为什么不是「小条先切」

`小条先切` 的排序赌注（分析报告 §3 ✅）是：把最脆弱的一刀安排在密封最好的时刻。
这个方向是对的，但它只**挪动了风险发生的时间**，没有消除风险 ——
小条一被切开就立刻成为自由件，要在低吸力下撑到整张板加工完毕。

HBT 的两趟结构让 pass 1 结束时**没有任何自由件**，
而 pass 2 全场只有 4% 负载。风险窗口从「整段加工时间」压到「pass 2 里的几秒」。

### 2.3 HBT 评分（替代四方向位掩码与 260mm 硬阈值）

不再用「有没有邻件」的布尔判断，改用两个连续量的加权：

```
对每个件 i：
    leak_i  = P_i                      # 释放后新增的泄漏周长（∝ P_i × 切深）
    expos_i = 无邻件的边数 E_i          # 位移自由度：暴露边越多越容易被推走

    score_i = w1 * norm(leak_i) + w2 * norm(expos_i)
    pass 2 按 score_i 升序   # 泄漏小、被包夹得紧的先释放
```

邻接图 `G`：`i ~ j` 当两件包围盒间距 < 刀具直径 + 余量。

**注意这里与 `小条先切` 的结论是一致的**：小条恰好 `leak` 小（周长短）、
`expos` 小（夹在大板之间）→ 两项都低 → 仍然先切。
但 HBT 靠的是连续评分而不是 `SmallWidth ≤ 260mm` 这个硬阈值，
所以 280mm 的件不会因为差 20mm 就掉进另一条代码路径。

同时保留一个**与现有实现相反**的补充规则 ⚠️(需实机确认)：

> 对 `expos_i ≥ 3` 的件（三面或四面暴露，例如排版外缘的散件），
> 不要指望「先释放」——它们释放后位移自由度太高。这类件直接走 §2.5 微连接。

### 2.4 末端负载整形（力约束进给）

`§1.1` 的归零是**几何**保证的；这一步再加一层**力**保证：把最后一段的进给压下来。

```
保持力（标定式）:  F_hold(i) = A_i * Δp * η
                   Δp = 真空表压(MPa)   η = 有效系数(垫板渗透/密封损失), 建议初值 0.3, 需标定
切削力（估算）:    F_cut ≈ Kc * ap * ae * fz * Z * (D 相关)
                   ap = 轴向切深, ae = 径向切宽(=D 满槽), fz = 每齿进给, Z = 齿数

对每条分离刀的最后 n mm（建议 n = max(5mm, D)）:
    F(t) 从 F0 线性降到 F_min
    约束:  F_hold(i) / F_cut(t) ≥ M_min     推荐 M_min = 3 ~ 5
    → 解出 F_min，若 F_min < 300mm/min 则该件应升级为微连接
```

好处：这是**可标定**的，不是拍脑袋。

**标定办法（半天工作量）**：
1. 取一个已知会松的件型（如 100×60），固定其它参数，逐档提高 `CutFeed` 直到它在切削中移动；
2. 记录临界的 `F0*`，由 `F_hold = F_cut(F0*)` 反解 `η`（`Kc` 取同材料文献值或再用一组试验）；
3. 以后换材料只需重取 `Kc`。

### 2.5 微连接（对最小件兜底）

```
对 A_i < A_crit 或 expos_i ≥ 3 的件：
    k_i = clamp(round(P_i / 120), 2, 4)     # 按周长决定连接点个数
    每段连接长 3~6mm，留皮厚度同 s
    → 这些件在任何一趟里都不会被切离
    → 最后单独一趟切连接段（同刀具、低进给）或人工掰断
```

### 2.6 完整伪代码

```
输入: 件几何 {A_i, P_i, 轮廓路径}, 料厚 T, 刀具 D/Z, 真空压 Δp, 分区图, Kc/η

1. 分类:  每条闭合刀路 → 分离刀 / 非分离刀
2. 邻接:  建图 G, 算 E_i, leak_i, score_i
3. 分趟:
      pass1: 所有分离刀, FinalDepth = -(T - s)
      pass2: 所有分离刀, FinalDepth = -(T + 0.3)
      非分离刀: 随 pass1 一次到位(它们不释放任何件)
4. 排序:
      pass1: 空行程最优(最近邻 / 按排版轴向扫描)
      pass2: score_i 升序, 同分区连续切完(减少阀门/泄漏反复)
5. 下刀:
      全部斜坡, 锚点 = 段末(起点前 sloopDist 处), 角度 10°(18mm → sloopDist≈102mm)
      受边长限制时退化螺旋, 用 AlphaCAM 原生 AutoZRampAngle / HelicalInterpolation
      ⛔ 斜坡绝不能锚在起点(否则负载反向, 切离落在满负载上)
6. 进给:
      pass1: 原工艺 F0
      pass2: 每条分离刀最后 n mm 按 §2.4 降到 F_min
7. 兜底:  §2.5 的件改为微连接, 单独一趟
8. 输出:  两趟工序(同刀同刀号) → NC
```

---

## 3. 与现有方案对比

| 维度 | 小条先切 | modRamp.bas | **HBT** |
|---|---|---|---|
| 建模对象 | 几何邻接位掩码 | 排版板中心距离 | **负载 / 保持力预算** |
| 分离刀处理 | 小条有斜坡，**大板无** | 只处理小板件 | **全部件，无例外** |
| 释放瞬间 | 靠斜坡归零 ✅ | 靠斜坡归零 ✅ | 归零 + **负载仅 4%**（双保险）|
| 释放之后 | 自由件暴露整段加工时间 | 同 | **不存在自由件窗口** |
| 排序依据 | CutPriority（邻件数）| 距板心距离 | **leak/expos 连续评分** |
| 小件判定 | `≤260mm` 硬阈值 | `minSize` 硬阈值 | 连续评分 + `A_crit` |
| 进给 | **硬编码覆盖** 24000/9000/2000 | 读原工序 ✅ | 读原工序 + **力约束整形** |
| 最小件兜底 | 无 | 无 | **微连接** |
| 可标定 | 否 | 部分 | **是（η / Kc / M_min）** |
| 时间代价 | 基准 | 基准 | **+10~25%**（见 §5）|

---

## 4. 为什么它在原理上更干净

1. **它承认了唯一有效的机制并推广它** —— 归零不是「小条专用技巧」，
   而是所有分离刀都该有的性质；大板同样需要。
2. **它把「排序难题」降级了** —— 两趟结构下 pass 1 顺序与保持力无关，
   于是不需要再猜「小条先切对不对」。
3. **它给出的是可标定模型**，不是阈值。换材料/换刀具/换垫板后，
   重新标定 η 即可，不必重新调参。
4. **它显式处理了「释放之后」这个被两套现有实现都忽略的窗口** ——
   而这恰好是用户现场症状（「跑起来一会儿才松」）最可能对应的区间。

---

## 5. 代价与失败模式（必须一起考虑）

| 风险 | 说明 | 缓解 |
|---|---|---|
| **时间 +10~25%** | pass 2 多一趟 Z 升降与空行程；但只切 0.8mm，pass 2 进给可提到 1.5~2× | 用 pass 2 提速抵消；实测后评估 |
| **切屑堆积** ⚠️ | 留皮时切缝底部闭合，切屑无处排出，18mm 深槽可能挤屑、反而给件侧向力 | s 取 0.8~1.2mm 足够厚；pass 1 后加吹屑/吸屑；pass 2 高进给快切 |
| **薄板比例失真** | 3mm 板留 0.8mm 就是 27% | `s = min(0.8, T * 0.15)` |
| **翘曲** | 通槽切开后内应力释放会翘曲，反而更容易被顶 | 留皮本身抑制翘曲，是加分项 |
| **加工道次关联** | 你们 CDM 流程有「道次窗口」，两趟不能破坏道次关联 | 两趟用**同一刀号、同一工序**的两次走刀，或新增道次但保留原道次 |
| **真空极差时仍失败** | 若整机真空度本身不达标，pass 2 也会松 | §2.5 微连接兜底；但根因是机器（分析报告 §5.5）|
| **~~`StockZ` 语义未验~~ 已关闭** | ~~用 AlphaCAM 原生留料可能与我预期不同~~ **原文实为「Z Stock for Z-Contour roughing」，不是通切留皮参数** | 改用 `NumberOfCuts` / `DepthsOfCutSpecified` / `ThicknessLastCut`（见分析报告 §9） |

---

## 6. 分阶段落地

| 阶段 | 内容 | 改动量 | 验证方式 |
|---|---|---|---|
| **1** | 手工把现有切断工序**复制成两条**，第二条 `FinalDepth` 再深 0.8mm；其余不变 | 零代码 | 直接上机跑一版，看小件是否还松 |
| **2** | 在 VBA/MCP 里实现 §2.3 评分排序 + §2.5 微连接；下刀改用原生 `AutoZRampAngle` | 一个模块 | 对比松动率与加工时间 |
| **3** | §2.4 力模型标定 + 末端进给整形（可放在 NC 后处理层）| 中等 | 标定试验 + A/B |
| **4** | 分区感知调度（需你们的真空分区图）| 待定 | — |

**不要跳过阶段 1**：它一次性验证了 §1.2 的物理假设（留皮是否真的保住吸力）。
如果阶段 1 就解决了问题，后面的复杂度可能都不必要。

---

## 7. 需要你确认/标定的输入

1. ~~**`StockZ`（留料厚度）与 `RoughFinish()` 的实际交互**~~ —— **已由 ACAMAPI 文档关闭**（见分析报告 §9）：
   留皮的正确原生参数是 `NumberOfCuts=2` + `DepthsOfCutSpecified=True` + `ThicknessLastCut`，
   与 `StockZ` 无关。
2. **真空表压实际值 Δp**（机器表读数）与垫板类型（MDF 密度/厚度）—— 用于 η 初值。
3. **你们的真空台是否分区可控**、排版件是否跨区 —— 决定 §2.6 第 4 步与阶段 4。
4. **一件典型的「会松」小件的尺寸与当时进给** —— 用于 §2.4 的标定起点。
5. **现有 NC 里 pass 1 需要多少时间** —— 用于评估 +10~25% 是否可接受。

---

## 8. 如果只能改一件事

**做阶段 1（两趟留皮）。**

理由：斜角下刀、选下刀边、排序策略，全部加起来也只影响「切离的那一刻」附近的几百毫秒；
而「件切透之后是否还吸得住」决定了**剩下的整段加工时间**。
现有两套实现都在这几百毫秒上做文章，**没有一套处理「切透 → 加工结束」这段窗口**。
阶段 1 用零代码改动把这段窗口整个消掉，是投入产出比最高的一步。

---

## 9. 落地状态（2026-09-13）

### 已落地：`CCC功能/modRamp.bas` v2.0

| 方案条目 | 落地情况 | 验证 |
|---|---|---|
| §1.1 末端归零：推广到**全部**分离刀 | ✅ `minSize = 0` = 处理全部闭合刀路 | 编译通过；标记断言 |
| 末端锚定的**禁止事项**写成代码注释 | ✅ 模块头 + 内联 | 标记断言 |
| §2.3 HBT 评分排序 | ✅ `score = 归一化(轮廓长度) + 归一化(暴露边数)`，升序 | 标记断言；旧 `bestDist` 逻辑确认消失 |
| §2.5 微连接（留连接点） | ⚠️ 代码已就位，**默认关闭**；UI 未加 | 编译通过；**功能未实测** |
| §2.4 小件降速（力模型的分档近似） | ⚠️ 已就位，**默认关闭** | 编译通过；**功能未实测** |
| 起点选择「选归零边」 | ✅ 改为直接算「朝向排版中心侧较长边中点」，修掉 v1.x 的越界跳变 | 标记断言 |
| 只处理闭合刀路 | ✅ 开放路径跳过并计数上报 | 标记断言 |
| 新增 `RampVersion()` 编译探针钩子 | ✅ | `tools/ccc_probe.py` → PASS |

**验证证据**：`running_snapshot → audit → deploy`（读回校验 `verify=True`，344 → 891 行）
→ `tools/ccc_probe.py` **RESULT=PASS**（整工程编译 + 16 项标记 + 3 项 ABSENT）。

**未做功能实测**：执行会删除并重建刀路，而当前 AlphaCAM 会话里有一张**未保存的图纸**
（`Modified=True`、`Name` 为空），不能在用户会话里跑。因此：
- 两个新功能都**默认关闭**，`frmRamp` 现有调用路径完全不变（向后兼容）；
- 模块自带 `App.SetUndoPoint`，一次 Ctrl+Z 可整体撤销；
- 功能验证需要一个测试排版图（见下）。

**已知限制（沿袭 v1.x）**：`CCC_RampDone` 幂等标记写在**被删除的旧路径**上，无法标记新刀路，
所以**对同一张图重复执行会重复处理**。完成提示里已写明警告。

### 落地后的修订（v2.1.0，2026-09-13，按用户要求）

| 项 | 结果 |
|---|---|
| **微连接（留连接点）/ 留皮** | ⛔ **已按用户要求整体移除** —— 模块里删掉了 `BuildTabWindows` / `AddContourWithTabs` / `InTabWindow` 三个过程与 `TAB_*` 常量、参数；窗体上删掉了 `chkTabs` / `lblTabStock` / `txtTabStock` 三个控件。modRamp 从 930 行缩到 **750 行** |
| **加工道次回填（新增）** | ✅ 用 `MillManualToolPath.Finish` 返回的 `Paths` 把新刀路的 **`OpNo` 设回原道次**，全部处理完再调 **`drw.Operations.OrderAll`** 重排（ACAMAPI：`"OpNo - Operation number of this path. Call Operations.OrderAll if this is changed"`）。这一条直接解决了"重建后刀路丢了原道次"的问题 |
| **幂等标记（修复）** | ✅ 原先 `CCC_RampDone` 写在**被删除的旧刀路**上，标记丢失 → 同一张图重复执行会**重复做斜坡**。v2.1.0 改写在 `Finish` 返回的**新刀路**上（`Paths` 可能是多条，逐条打） |
| **窗体控件数** | 14 → **11 个**（只留 `chkSlowSmall` 小件降速） |
| **刀具记忆** | ⛔ **不再记忆、也不预选**（`cmbMethodTool.ListIndex = -1`）—— 每次必须手动选，避免拿上次的刀跑错活；其余参数（深度/角度/范围/小件降速）照旧记住 |
| **撤销点** | ✅ `ApplyRampEntry` 在**任何改动之前**调 `App.SetUndoPoint`（并先 `SetUndoCommandName "斜角下刀"`）。ACAMAPI 原文："Commands in the macro following this one (up to the next call of SetUndoPoint) will be undone if the Undo command is used." → **一次 Ctrl+Z 整体回退** |

> 部署顺序也踩了一次：modRamp 的参数从 9 个减到 7 个，**必须先部署窗体、再部署模块**
> （VBA 允许"少传可选参数"，所以中间态可编译；反过来模块先减参数、窗体还传 9 个则会编译失败）。

### 待做

1. ~~**`frmRamp` 窗体加控件**~~ ✅ 已完成（2026-09-13），见上表。
2. **功能实测**（已用 2 件测试图跑通，见 §10；仍建议在真实排版图上复跑一次）。
3. **阶段 1 两趟留皮**：**这条现在更重要了** —— 既然微连接已从 modRamp 移除，
   「不让件在加工中被切离」只能靠 **§11 的原生参数路线**
   （`NumberOfCuts` / `DepthsOfCutSpecified` / `ThicknessLastCut`），且它不动原刀路、
   不破坏道次关联。
4. **力模型标定**（η / Kc），把小件降速的分档近似换成真模型。

> ⚠️ **落地后新增的一条注意事项（实测）**：`modRamp` 用 `ManualToolPath` 重建刀路后，
> **所在子工序名会变成「手动输入」**（原「精加工」不再出现）→
> 再跑一次时按"加工方式"筛选会失效，叠加"幂等标记写不进去"，**很容易把已做过的刀路再做一遍**。
> 详见 `VBA操作问题记录.md` §7.9(f)。

### 顺带修好的工程问题

- `tools/component_deploy.py`、`tools/running_snapshot.py` 原来**硬编码只认 CDM 工程**，
  CCC 工程根本部署不了 → 增加可选 `[Project]` 参数（默认仍是 CDM，旧用法不变）。
- 新增 `tools/ccc_probe.py`（CCC 工程的编译探针 + 标记断言），对应 CDM 的 `deploy_probe.py`。
- 发现并规避：**VBA 会重写长小数字面量**（`0.0174532925199433` → `1.74532925199433E-02`），
  会让部署读回校验永远失败。v2.0 改用运行时计算 `Pi = 4 * Atn(1)`。
- 受保护的工程（如现在的 CDM）会让 `running_snapshot.py` 抛裸 traceback →
  改为明确提示「先解除保护」。
- **`win_ctl.py --dismiss` 对 AlphaCAM 的 VBA `MsgBox` 无效**（`WM_COMMAND/IDOK` 发 8 次都不关），
  改为对 `确定` 按钮发 `BM_CLICK` 即关；且**不能按窗口类匹配**（会误伤其它程序）。
  详见 `tools/README.md`。

---

## 10. 功能实测（在 AlphaCAM 里真跑，2026-09-13）

### 测试台

两件：A `400×300`（周长 1400）、B `600×300`（周长 1800），X 向间隙 5mm（< 刀径+2，互为邻件）；
刀具 `FLAT - 5MM`；一个 `精加工` 工序、两条闭合刀路、深度 -18。

### 方法

- `App.Run` 最多 10 个位置参数，而 `ApplyRampEntry` 有 9 个可选参数 + 宏名 → 改为
  **注入临时 VBA 模块**再运行（`Public Sub MCPRun()`）。
- 回执框交给**独立监视进程**（按标题精确匹配 + `BM_CLICK`），它同时把回执内容记下来当结果摘要。
- 逐元素读 `Element.StartZL/EndZL` 校核 Z 剖面（文档确认 `StartZL/EndZL` = 局部 Z）。

### 结果（v2.0.2，rapid 段已排除）

| 用例 | 结果 |
|---|---|
| **T1 斜坡（无微连接）** | 两件均 **210/211 元素**，斜坡 **205 元素 / 103.72mm**、Z 单调降到 -18、满深段 6 元素、连接点平段 **0**（正确）。<br>A 的斜坡起点 (302.083, 300) 与闭合点 (200, 300) 坐标差 **102.083mm = 18/tan(10°)** → **斜坡精确锚定末端** ✅<br>（元素长度和 103.72mm 比理论 102.08mm 多 1.6%，因为第 205 个元素是收尾衔接段；坐标差才是精确判据。） |
| **T2 微连接 0.8mm** | 元素 +16（= 4 个连接点 × 4 个发射点）；**平段 4 个 / 20.00mm**（4 × 5mm）@ Z=**-17.2** ✅；满深段 6 → 14（+8 = 4 × 2 个过渡点）✅；**斜坡元素数 205 → 205，不受连接点影响** ✅ |
| **T3 留皮 1.2mm + 小件降速** | 平段 Z = **-16.8**（= -18 + 1.2）✅；**小件降速 2 条**，按件面积 120000 / 180000 mm² 各 ×0.7 ✅ |
| 起点合法性 | A=(200,300) 在 400×300 上边中点、B=(705,360) 在 600×300 上边中点 —— 都是**路径上的点**，v1.x 的越界点不再出现 ✅ |
| rapid 段 | 第 2 件含 1 个 rapid（610mm，AlphaCAM 自动加的定位段），modRamp 复制几何时**正确跳过**（`If Not elem.IsRapid`）✅ |

### 测试抓出的两个真实缺陷（已修）

| 版本 | 缺陷 | 触发方式 |
|---|---|---|
| v2.0.1 | **无排版的图纸整体失败**：`drw.GetNestInformation` 抛「现在图档内无排版」，而 `SetGeoStartToSheetSide` 里"退回图纸范围中心"的兜底永远走不到（v1.x 遗留） | 单件/非排版图直接报错框 |
| v2.0.2 | **HBT 的"泄漏周长"与降速的"面积"把 rapid 段算进去了**：原来用 `tp.Length` / `tp.MinXL~MaxXL`，实测 `bbox=[0,0,1005,300]` 而 `GetFeedExtent=[405,0]-[1005,300]`。导致第 2 件被误算成 301500mm²（应 180000）→ 只降速 1 条 | 刀路含快速定位段时 |

> 诚实记录：测试过程中我先后 3 次判读错误（把"沿边跳越界"当成能工作、
> 把 rapid 段当成几何被合并、把 rapid 长度算进斜坡长度），**每次都是我的判读脚本错、
> modRamp 的行为是对的**。这也说明"读回逐元素 Z 剖面"这个验收手段是必要的 ——
> 只看 `MsgBox` 的计数会漏掉真缺陷，只看长度会被 rapid 带偏。

### 第 2 / 3 轮实机反馈（v2.1.1 → v2.1.2，2026-09-13 ~ 09-14）

用户在真机上提了三条，逐条核实结果如下（**其中一条是我的验收脚本错，不是插件错**）：

| # | 用户反馈 | 核实结论 | 处理 |
|---|---|---|---|
| A | **顺序没有从外往内** | ✅ **真缺陷**（v2.1.1 引入）。基准中心取错：`Drawing.GetExtent` 的参数顺序是 `(X1,Y1,Z1,X2,Y2,Z2)`，第 3 个是 **Z1**；我按 `(x1,y1,x2,y2,0,0)` 调用 → X2 位拿到的是**负的切深**、Y2 位拿到真正的 X2，"中心"变成 `(-10.25, 800)`。距离几乎全被 X 支配，排序退化成**原顺序倒序**（实测 `700,250,100,600`，与错误中心的预测**逐位吻合**）。详 `VBA操作问题记录.md` §4.9 | **v2.1.2** 修正参数顺序 + 取回后校验 + 候选件包围盒中心兜底 |
| B | **切完要抬刀到 Z20** | ⚠️ **功能其实已经生效**（v2.1.1 就对了），错的是**我的验收判据**：`MillManualToolPath.Finish` 会把收尾的 `Add3DRapid` **单列成一条 1 元素刀路**，而我只查了"每条刀路末元素"，于是把轮廓刀路（末元素 Z=-18）判成失败。详 `VBA操作问题记录.md` §7.9(g) | **不改插件**，改判据（按子工序内所有刀路拼接看末尾） |
| C | **加工道次窗口没更新** | ✅ **真缺陷**。`Frame.ProjectBar` 就是加工道次窗口，v2.1.0 只恢复了 `ScreenUpdating`，没管它 → 用 `Frame.ProjectBarUpdating = False/True` 包住整个修改过程（错误分支也恢复） | **v2.1.1** 修复，v2.1.2 复测通过 |

**v2.1.2 真机验收**（4 件硬编码在 `0..1600 × 0..300`，参考中心 = 图纸范围中心 `(800,150)`；
`GetExtent` 实测 `[-2.5,-2.5,-18,1602.5,302.5,5]` → 中心确为 `(800,150)`）：

| 判据 | 期望 | 实测 |
|---|---|---|
| **从外往内** | 距离序列单调不增 | `700, 600, 250, 100` —— 与期望**完全一致** ✅ |
| **每条子工序末尾抬刀** | `rapid=True, EndZL=20`，且恰 1 条 | **4/4 子工序**全部 OK ✅ |
| **跨件交接高度** | 下一件首元素是 `Z 20 → 0` 的 rapid | sub1→2、2→3、3→4 全部 OK ✅ |
| **仍切到深度** | 最深处 = -18 | 4/4 ✅ |
| **`ProjectBarUpdating` 复位** | True | True ✅ |
| **窗口内容（视觉截图）** | 由 1 行变 4 行 | `Op 1` 下 **4 行** `手动输入 刀具 2 FLAT - 5MM` ✅ |
| **道次 `OpNo` 回填** | 全部 = 1 | `[1,1,1,1,1,1,1,1]`（4 条轮廓 + 4 条抬刀）✅ |
| **二次执行幂等** | 全部跳过、刀路结构不变 | 弹窗报 `候选=8, 已处理过=8`；复跑前后 8 条刀路的**元素数与 `OpNo` 逐条相同**（210/211 + 1 条抬刀）✅ |

> 诚实记录（第 2 次）：这三条里**有一条是我的判据错**。加上 §10 那次，
> 到目前为止 4 次"看起来像插件的 bug"，**2 次是我的检测脚本错**、
> 2 次是真缺陷（`GetExtent` 参数顺序、`ProjectBarUpdating` 漏恢复）。
> 结论不变：**先用只读手段把"我以为的判据/我以为的取回值"打印出来核对，
> 再决定改不改代码** —— 否则会把好代码改坏，或者去修一个不存在的 bug。

### 第 4 轮实机反馈（v2.2.0，2026-09-14）：未匹配小板件条件的刀路要**排到小板件之后**

**用户要求**：同一个【加工方式 + 刀具】里，**没有匹配到小板件条件的刀路，要排到小板件后面切割**
（小条先切，大件让位）。

**先量了现状**（3 件测试台：大A 400×300 / 小B 100×300 / 大C 400×300，`minSize=200` 只匹配小B）：

| | 全局刀路顺序 | 结构 |
|---|---|---|
| 处理前 | 大A, 小B, 大C | `op1/sub1` 三条 |
| 处理前(v2.2.0 之前) | 大A, 大C, **小B** | `sub1`=大A+大C(原「精加工」), `sub2`=小B+抬刀(新建) |

即 `ManualToolPath` 新建的子工序被**追加在原子工序之后** → 小板件反而**最后**切，
与"小条先切"正好相反 —— 用户反馈属实。

**两个机制（都是文档里挖出来的）**：

1. **`Drawing.OrderManual(Ordered)`** —— 重排加工顺序
   （文档：*the paths will be ordered to match the order of the paths in this collection*）。
   分组键 = `OpNo + 加工方式名 + 刀具号`；每组**先放已处理(小板件)、再放未匹配**。
   只在顺序确实需要变化时才调用（逐条比较对象同一性）。
2. **`md.Attribute("LicomUKDMBOperationName") = 原加工方式名`** —— 让新建子工序
   **保留原名**（不再是「手动输入」）。这既修复了 §7.9(f) 的老问题，
   也让上面那个分组键对"已处理/未处理"两条路径统一（否则两边的子工序名不同、分不到一组）。

**实测澄清：`OrderManual` 不会改坏刀路。** 逐元素指纹对比：

| 项 | 结果 |
|---|---|
| 斜坡段元素数 / 逐段 Z 剖面 / 锚定关系 | **逐元素不变** ✅ |
| 被排到**首位**那条 | 去掉引导 rapid(212→211) —— AlphaCAM 对"首条刀路"的固有约定 |
| 排到后面的每条 | 补一条连接 rapid(4→5) |
| 自定义属性 `CCC_RampDone` | **存活** → 幂等标记没丢（复跑实测 `候选=6, 已处理过=6`）✅ |
| 子工序 | 按新顺序重新分组，子工序名不变 |

**踩到的坑（记下来免得重犯）**：我第一版实验把"轮廓刀路"和它配对的**抬刀 rapid 刀路拆开**
（把抬刀排到最后），AlphaCAM 为自洽去补/挪 rapid → 轮廓少 1 个元素、抬刀涨成 2 个、
多出一条 `Z=0` 横穿 rapid，看起来像"刀路被改坏了"。
**规矩：重排必须保持"轮廓 + 其配对收尾 rapid"成组不拆。**

**顺带清掉的第二个 `GetExtent` 坑**：这一轮发现 `drw.GetExtent` 的 `Y2` 会返回
**确定性错值**（真实 302.5，它给 2679.53，连调 5 次一样），会把"从外往内"的距离基准带偏。
于是**彻底不用 `GetExtent`**，兜底参考点改为
**板件几何中心 → 候选件联合包围盒中心** 两级。详见 `VBA操作问题记录.md` §4.9.1。

**v2.2.0 真机验收**（同上 3 件测试台，`minSize=200`）：

| 判据 | 期望 | 实测 |
|---|---|---|
| **顺序** | 小板件 + 其抬刀 在未匹配之前 | 全局 = `小B(210) → 抬刀(1) → 大A(5) → 大C(5)` ✅ |
| **命名** | 新子工序名 = 原加工方式名 | `精加工   刀具 2   FLAT - 5MM`（无「手动输入」）✅ |
| **刀路本体** | 斜坡仍锚定闭合点、切深 -18、子工序末尾 Z20 | 锚距 **102.08mm**（= 18/tan10°）、最深 -18、末尾 rapid Z20 ✅ |
| **大件** | 补上连接 rapid | 4→5 元素，全部含引导 rapid ✅ |
| **道次 OpNo** | 全部 = 1 | `[1,1,1,1]` ✅ |
| **ProjectBarUpdating** | 复位 True | True ✅ |
| **幂等（同参数复跑）** | 全部跳过、结构不变 | `候选=6, 已处理过=6`，6 条刀路逐条元素数与 OpNo 不变 ✅ |
| **回归：minSize=0 全处理** | 仍"从外往内" | 距离序列 `700,600,250,100` 与期望逐位一致 ✅ |

> 诚实记录（第 3 次）：这轮我又踩了**两次自己的判据错**——
> ① 复跑脚本传 `minSize=0`，把"未匹配件被正常处理"误判成"幂等失效"；
> ② 又一次把"子工序内抬刀被单列成独立刀路"当成"抬刀没生效"（§7.9(g) 写过的同一个坑）。
> 累计：5 次"看起来像插件的 bug"里 **3 次是我的检测/判据错**，2 次是真缺陷
> （`GetExtent` 参数顺序、`ProjectBarUpdating` 漏恢复），另有 1 次是**同一 API 的第二个坑**。
> 结论仍然只有一条：**先把"我以为的"打印出来核对，再决定改不改代码**。

### 第 5 轮：真实排版工件体检（2026-09-14，用户在真实工件上跑过 v2.2.0）

**用户要求**：检查已执行小条斜切的档案、看加工道次窗口的异常。

体检对象：真实排版工件（104 几何 / 10 工序 / 80 刀路 / 2 个版件），其中**工序 5 与工序 10**
（`底8高22.8倒角刀`）已被本插件处理 —— 共 **20 条**刀路（子工序名仍是 `精加工`、不是`手动输入`，
说明跑的是 v2.2.0）。

**体检结论（真问题）**：

| # | 现象 | 性质 |
|---|---|---|
| 1 | 加工道次窗口（按**版件**分组）出现 **(不在版件中)**；签名交叉比对：**重建的 20 条一条都不在版件里，未动过的 60 条全在** | **本插件的结构性副作用**（`ManualToolPath` 造的新刀路不进版件表）。根因/证据/机制见 `VBA操作问题记录.md` §7.14 |
| 2 | 子工序数量爆炸：工序5 `1 → 7 个`，工序10 `1 → 4 个`（每件一个，名字全相同） | 同上（`ManualToolPath` 固有行为，§7.9(f)） |
| 3 | 每个被动过的子工序里多出一条**纯 rapid 的"抬刀"刀路**（1 元素、无进给段） | 功能等价（§7.9(g)），但窗口里多一行、报表里多一条 |
| 4 | **9 / 10 条**重建刀路含一个**零长重复元素**（斜坡末点正好落在闭合点上，随后那条"回到起点"的收尾线退化成零长） | **我造刀路时的小缺陷**：应当"没真的移动就不加元素" |

**已实施并真机验收（v2.3.0 / v2.3.1）**：把"删旧刀路 + 建新刀路"改成
**`原刀路.ReplaceElements(新元素)` 原地替换**，并用 `Path.AddPath` 把收尾抬刀并回同一条刀路。

| 判据 | v2.2.0 | **v2.3.0** |
|---|---|---|
| 刀路总数 | 70 → 80 | 70 → **70** ✅ |
| 子工序数（工序5/工序10） | 1 → 7 / 1 → 4 | **1 → 1 / 1 → 1** ✅ |
| 版件归属 | 20 条中 0 条在版件里 | **10/10 全在版件里** ✅ |
| 加工道次窗口 | 有 `(不在版件中)` | 该节点**消失**（截图核对）✅ |
| 抬刀 | 单列一条纯 rapid 刀路 | 同一条刀路**末尾** ✅ |
| 零长重复元素 | 9/10 条中招 | **0** ✅ |
| 斜坡锚定 / 切深 | 102.08mm / -18.06 | 67.4mm（角 15°）/ -18.06 ✅ |
| 同组顺序（小板件在前） | ✅ | ✅ |
| 幂等（同参数复跑） | ✅ | `已处理过=10`，结构不变 ✅ |

**顺带修掉的一个隐蔽坑（v2.3.1）**：深度门槛的浮点比较 —— 版件厚度 18.06 存成单精度后
刀路 `FinalDepth = 18.059999465942383`，用户按名义值 18.06 填门槛时会**一条都不受理**
（提示只说"深度不足=42"）。现在门槛带 0.05mm 容差。

**还没定的问题（需要用户确认，不影响本次修复）**：

1. 版件归属现在**保住了**，但"(不在版件中)"从 v2.0 起就存在 —— 如果你们后处理里有任何
   按版件输出的逻辑，值得顺手核对一下现在的结果是否与预期一致。
2. 那个"每件一条刀路"的形态与传统 `RoughFinish` 生成的刀路在窗口里看起来一致了，
   但如果后处理对"手改过的刀路"有特殊假设，建议先试切一件确认。

> 诚实记录（第 4 次）：这轮我**又踩了一次自己的错**，而且这次踩在用户的真实工件上 ——
> 以为 `App.CreateTempDrawing` 能当刀路沙箱，结果测试刀路落进了真实图纸
> （多出一个工序 11），已按"工序号 + 刀具 + 坐标"判定并删除还原（10 工序 / 80 刀路 ✓）。
> 另有一次"净零"实验（`Path.Copy` + `ReplaceElements`）动了 1 条刀路的 `Closed`/`ToolInOut`：
> `ToolInOut` 已写回原值 0，`Closed` 只读改不回来（元素未变）。
> **教训：在用户正在编辑的真实图纸上做实验，"我以为的非破坏性"不算数，要先确认 API 的作用域。**

---

## 11. 更省事的一条路：原生参数（**建议先试这条**）

查 ACAMAPI 文档后确认（分析报告 §9），下面这些**不需要写任何代码**，只是工序参数：

| 目的 | 参数 | 说明 |
|---|---|---|
| **两趟留皮** | `NumberOfCuts = 2` + `DepthsOfCutSpecified = True` + `ThicknessLastCut = 0.8` + `FinalDepth = -(料厚+0.3)` | 末层只切 0.8mm —— 等价于 §2.1 的 pass1/pass2，但**在同一工序内完成、不动原刀路、不破坏道次关联** |
| **原生斜坡** | `AutoZ = True` + `AutoZRampAngle = 10` + `AutoZMaxDepthOfCut` / `AutoZMinDepthOfCut` | 按角度给，不用手工反算 `sloopDist` |
| 环绕入刀 | `LoopRadius` | 短边放不下斜坡时用它替代 |
| 预钻入刀 | 先钻孔 + Pre-Drilled-Holes 选项 | 官方示例明确推荐（"drill holes that can be used to plunge into"），彻底避开板面直插 |

**结论：§2.1（两趟留皮）与 §2.2（斜坡）很可能可以用参数解决，
不必依赖 modRamp 的手工刀路重建。** 建议的验证顺序因此调整为：

1. 先在一张测试排版上把 `NumberOfCuts=2` / `ThicknessLastCut` / `AutoZ` / `AutoZRampAngle`
   调出来，**看 NC 里的 Z 是否真的分成两层、是否有斜向入刀段**。
2. 若成立 → 生产直接用参数；modRamp 退化为「需要**逐件连接点**与**逐件降速**时的补充」
   （原生参数无法按件给不同的留皮/进给）。
3. 若不成立（例如原生斜坡只对分层模式有效、单趟通切用不上）→ 再用 modRamp v2.0.2。

MCP 桥已经把这三个参数映射好了：
`number_of_cuts` → `NumberOfCuts`、`auto_z` → `AutoZ`、`auto_z_ramp_angle` → `AutoZRampAngle`
（见 `alphacam_com.create_mill_data` 的 prop_map），`run_machining` 直接能传。

> ⚠️ 需实机确认的一点：原生斜坡挂在「automatic **multi-level** cuts」模式下，
> 单趟通切到底的工序能否启用，文档没写。这正是上面第 1 步要验的。


