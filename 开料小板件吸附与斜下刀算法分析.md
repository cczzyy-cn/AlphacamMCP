# 开料小板件真空吸附与「斜角下刀 / 选下刀边」算法分析

> 分析日期：2026-09-13
> 对象：`小条先切` 插件（VB.NET/WPF，dnSpyEx 反编译） + 本项目 `CCC功能/modRamp.bas`
> 目标：解释两个算法的机理，并回答「开料时真空泵吸不住小板件、板件松动」

---

## 0. 结论先行

| 问题 | 斜角下刀能不能解决 | 说明 |
|---|---|---|
| 入刀瞬间把件顶走/掀起 | **能** | 把 Z 向直插的轴向力转成渐进的侧向力 |
| **切离那一瞬间被推走** | **能 —— 这是它最被低估的价值** | 斜坡让最后 `sloopDist` 的吃刀负载**线性衰减到零**，而轮廓恰好在归零点闭合（见 1.3） |
| 件切离**之后**被后续刀具/振动带跑 | **不能** | 那时件已自由，斜下刀管不到 |
| 真空泄漏导致吸力不足 | **不能** | 属于台面/分区/垫板/排板问题 |

**一句话**：斜角下刀管的是「**从入刀到切离**」这一段 —— 入刀不冲击 + 切离时负载归零；
选下刀边管的是「**负载在哪条边归零**」；而切离**之后**的失稳与真空泄漏它管不到，
那要靠留皮/微连接、真空分区与垫板。上一版报告把斜下刀的作用窄化成「只解决入刀冲击」，是错的。

---

## 1. 算法一：斜角下刀（`小条先切/CutToolPath.vb` → `RoughFinish`）

### 1.1 入口与触发条件

```
RoughFinish(Acam, isSloopingLine, backDist, sloopingDist, pointDist)   ' CutToolPath.vb:274
  ├─ SetStartPointOfSmallPanel(backDist)      ← 先选下刀边（见第 2 节）
  └─ If isSloopingLine AndAlso sloopingDist > 0 Then   ← 默认 True / 20mm（Set.vb:108-109）
```

调用点只有一处：`DrwNest.OptimizeCutPaths()`，`list`（小条组）逐条调用，`pointDist` 硬编码 `0.5`：

```vb
list(k).RoughFinish(Me.Acam, Me.SloopingLine, Me.BackDist, Me.SloopDist, 0.5)   ' DrwNest.vb:232
```

### 1.2 斜坡是怎么生成的（浅切分支，<20mm 料）

以 `DrwNest.vb:346-377` 为准（深度 ≥20mm 是另一分支，见 1.5）：

```vb
num7 = ToolGeo.Length - sloopingDist          ' 斜坡起点：距路径末端 sloopingDist 处
PointAtDistanceAlongPathL(num7, num8, num9, element)   ' 求该点坐标
num10 = Floor(sloopingDist / pointDist)       ' 步数 = 20 / 0.5 = 40 步
mtp4  = millData3.ManualToolPath(num8, num9, 0.0)      ' ← 从 Z=0（板面）开始，不是从深度开始
num11 = FinalDepth / num10                    ' 每步下刀量
For m = 1 To num10
    PointAtDistanceAlongPathL(num7 + pointDist * m, …)
    mtp4.imethod_0(num8, num9, num11 * m)     ' 沿路径前进 0.5mm，同时下降
Next
mtp4.imethod_0(GetFirstElem().StartXL, StartYL, FinalDepth)   ' 回到几何起点（闭合则零长度）
For n = 1 To elements3.Count                  ' 满深度走完整圈
    …
```

三个关键点：

1. **斜坡长度是固定距离，不是角度**。`SloopDist` 默认 20mm，与料厚无关。
2. **斜坡占用路径的「最后一段」**，即在返回起点之前的 `sloopDist` 毫米上下降，
   在**几何起点处到达满深度**。
3. **斜坡留下的是楔形槽而不是深坑** —— 这一点是理解整个设计的关键，见 1.3。

### 1.3 斜坡的真正价值：切离那一刻负载归零 ★

斜坡在路径上留下的槽，深度是「起点 0 → 末尾满深度」线性变化的**楔形**。
主切削段随后从几何起点（= 路径末尾）出发、满深度走完整圈，
它**最后**才经过这段被预切的区间，而在这段上它切掉的是楔形的**补集**：

```
沿路径位置 →   L-sloop                        L（= 几何起点，轮廓在此闭合）
斜坡已切深度     0  ─────────────►  满深度
主切剩余吃刀量  满深度 ───────────►  0        ← 线性递减
```

**所以主切削段的吃刀负载在最后 `sloopDist` 毫米上线性衰减到零，
而轮廓恰好在这个归零点闭合** —— 板件被切离的那一瞬间，刀具几乎不再吃刀。

这一条决定了三件事：

1. **斜角下刀的作用不止于消除入刀冲击。** 它同时把「切离时刻」变成整条刀路上
   **负载最小**的时刻。直插下刀做不到：那样负载从头到尾是满的，切离发生在满负载下。
   （上一版报告只写了「件在末尾才被切离，刀具随即抬起」，没写出这个归零机理。）
2. **末端归零是纯几何效应，与坡角无关。** 斜坡 20mm 或 200mm，末端都归零 ——
   所以 `SloopDist` 默认 20mm 偏陡，损害的是**斜坡段自身**的负载（见 1.4），
   而不是这个特性。两者要分开评价。
3. **「选下刀边」的真正含义由此明确**：起点决定轮廓在哪条边闭合，
   也就决定**负载在哪条边归零**。见第 2 节。

> ⚠️ 该特性只在**闭合刀路**上成立。开放路径时，收尾那行
> `Add3DLine(GetFirstElem().StartXL, StartYL, FinalDepth)` 会变成一条
> 横穿零件的满深度直线（见 1.5(c)）。`小条先切` 取刀路时 `includeOpen=False`，
> 只处理闭合路径，所以安全。

### 1.4 有效斜坡角 —— 默认值偏陡（实测换算）

有效角 = `atan(|FinalDepth| / SloopDist)`：

| 料厚 | SloopDist=20（默认） | 有效斜坡角 | 评价 |
|---|---|---|---|
| 12mm | 20mm | 31.0° | 偏陡 |
| 18mm | 20mm | **42.0°** | 接近直插，仅影响斜坡段自身负载 |
| 26mm | 20mm | 52.4° | 基本等于直插 |

行业经验：真空台面上木工铣削的斜向入刀宜取 **5°~15°**。
按 18mm 料、10° 计，斜坡长度应为 `18 / tan(10°) ≈ 102mm` —— 是默认值的 5 倍。
斜坡段越陡，刀具在坡底接近满深度时的瞬时轴向负载越大，越容易在**入刀段**
就把小条推走；但切离那一刻仍然归零。

> 这就是**你们自己的 `CCC功能/modRamp.bas` 更正确的地方**：它用角度作为输入
> （`modRamp.bas:190` `sloopDist = actualDepthAbs / Tan(rampAngle * DEG2RAD)`），
> 并且用 `If sloopDist > geoLen * 0.8 Then sloopDist = geoLen * 0.8` 兜住短边（`:194`）。

### 1.5 三个必须知道的缺陷

**(a) 除斜坡段外的所有下刀都是「直插」。**

| 分支 | 下刀方式 | 代码 |
|---|---|---|
| <20mm + 斜下刀 | 斜坡段 `ManualToolPath(…, 0.0)` → **正确** | CutToolPath.vb:361 |
| <20mm + `DepthsOfCutSpecified` | 先一趟半深：`ManualToolPath(…, finalDepth/2)` → **直插到半深** | :333-335 |
| ≥20mm + 斜下刀 | 先一趟 60%：`ManualToolPath(…, FinalDepth*0.6)` → **直插到 60% 深度** | :285-287 |
| 无斜下刀 | `<20mm` 直接 `MillData.RoughFinish()`；`≥20mm` 两趟直插 | :380-417 |

对 26mm 料（你们的 `MD26Z+8`），**第一刀是直插到 -15.6mm**，然后才做斜坡。
小条件在这一下就已经被顶松了。

> 补充：这一趟 60% 首刀是**整圈满周长切削且不带斜坡**，等于先把周边整体切开到 60% 深，
> 件随后只靠剩下 40%（26mm 料即 10.4mm）的薄圈挂着，再去跑带斜坡的第二趟。
> 也就是说 ≥20mm 料的分支把「件被削弱」提前了一整趟。虽然**最终切离**仍由第二趟的
> 斜坡归零（1.3 的特性还在），但首刀这一下的直插与整圈削弱是独立的额外风险。

**(b) 主轴转速与进给被硬编码覆盖。**

四个 ManualToolPath 分支全部写死：

```vb
millData.SpindleSpeed = 24000F : millData.CutFeed = 9000F : millData.DownFeed = 2000F
```

即**无视该工序原本的 S/F**。如果你们实际工艺不是 24000/9000/2000，
跑完「小条先切」后切削力就变了 —— 这本身就是板件被推走的一个独立嫌疑。
（`modRamp.bas:151-157` 改成先读原工序的 `GetMillData`，只在读不到时才回落到这组默认值。）

**(c) 斜坡长度不受边长约束。**

`小条先切` 没有 `sloopDist ≤ 路径长度` 的保护。若某条小条周长 < 20mm
（极窄的边角料），`num7 = Length - sloopingDist` 会为负，
`PointAtDistanceAlongPathL` 行为未定义。`modRamp.bas:194` 用 `geoLen * 0.8` 挡住了这件事。

---

## 2. 算法二：选下刀边（`DrwNest.SetCutPriority` + `CutToolPath.SetStartPointOfSmallPanel`）

这一部分是两级：**先判断四周有没有邻件**（得到方向位），**再据此把刀路起点挪到指定边的某个位置**。

### 2.1 第一级：四方向碰撞检测（`DrwNest.vb:490-641`）

对每条刀路 `j`：

```vb
' 取自身包围盒，四边各内缩 10%（得到「核心区」）
num3 = MinX + (MaxX-MinX)*0.1 : num5 = MaxX - (MaxX-num3)*0.1
num4 = MinY + (MaxY-MinY)*0.1 : num6 = MaxY - (MaxY-num4)*0.1

num  = 0   ' CutPriority：权重
num2 = 0   ' CutDirect ：方向位掩码（1=左, 2=上, 4=右, 8=下）
```

对另外每条路径 `k`，做四次（左/上/右/下）检查。以「左」为例：

```vb
If minY < num6 And maxY > num4 And minX < num5 Then      ' 包围盒粗筛：垂直重叠且偏左
    If maxX > num3 Then                                  ' 细筛：横向确实压过来
        path = 自身 ToolGeo 的副本
        path.ScaleL2(...)                                ' 缩放到「核心区」
        path.MoveL(-(num5-num3)/2.0, 0.0)                ' 整体向【左】平移半个宽度
        If Not path.TestIntersectPath(k.ToolGeo, 0, 0) Then GoTo 跳过
    End If
    记一位；num += If(Horizontal, 1, 3)                  ' 权重按「横/竖」给 1 或 3
End If
```

即：**把自身轮廓向左平移半个自身宽度，看会不会撞上邻件** —— 撞上就说明左边紧邻着别的刀路。
四个方向各做一次，得到 4 位掩码 `CutDirect ∈ 0..15`（15 种方向组合，这就是
`SetStartPointOfSmallPanel` 那个 15 路 if 的来源）。

> **诚实标注**：这段代码经反编译后是 GoTo 面条（`IL_0222` / `IL_01FE` 等标签），
> 位掩码的**极性**（位=1 表示「有邻件」还是「无邻件」）从文本上无法 100% 确证 ——
> `If Not flag Then` 与循环后的 `GoTo IL_0222` 组合有两种读法。
> 但它的**用途**没有歧义：决定 `CutDirect` 的取值，进而决定起点落在哪个角/边。
> 若要确证极性，最省事的办法是拿一个已知排版在 `小条先切` 里跑一遍，
> 再用本项目 MCP 的 `get_path_info` 读回刀路起点坐标反推。

权重 `num` 的设计：`num += If(Horizontal, 1, 3)`（左右方向）与 `If(Horizontal, 3, 1)`（上下方向）——
**与条料长轴垂直的那两个方向的邻居给更高权重**。`SortToolPathByPriority` 降序，
所以「四周被围得最死」的刀路先切。

### 2.2 第二级：把起点挪到指定边的指定位置（`CutToolPath.vb:422-667`）

```vb
Private Sub SetStartPointOfSmallPanel(backValue)          ' backValue = BackDist，默认 40mm
    If CutDirect <> 15 AndAlso CutDirect <> 0 Then
        ' 15 路分支：每路 4 种组合（Horizontal × CW），各 SetStartPoint 到一个角
        ' 例：CutDirect=1 → H&CW: (MaxX-back, MaxY) / H&CCW: (MinX+back, MaxY)
        '                  V&CW: (MaxX, MinY+back)  / V&CCW: (MaxX, MaxY-back)
        ' 共 14 路，覆盖 CutDirect = 1..14
    Else
        ' CutDirect = 0 或 15：不区分邻居，只按 Horizontal × CW 取两个角
        If Horizontal Then
            If CW Then SetStartPoint(MaxX - backValue, MaxY) Else SetStartPoint(MinX + backValue, MaxY)
        Else
            If CW Then SetStartPoint(MaxX, MinY + backValue) Else SetStartPoint(MaxX, MaxY - backValue)
        End If
    End If
End Sub
```

**先纠正一个理解偏差**：这一级的名字是「选下刀边」，但它真正在选的不是「从哪入刀」，
而是 —— 由 1.3 的归零机理 —— **「负载在哪条边归零」**，也就是**板件在哪条边上被切离**。
入刀点之所以要内退，只是顺带的次要收益。所以下面两条规则要按「归零边」来读。

两条规则合起来看：

- **起点落在角上，再沿相邻边内退 `BackDist`（默认 40mm）**。
  主效果：把**轮廓闭合点（= 负载归零点）**从角点挪到边的中段 ——
  角点是两条待切边的交点，是件上约束最弱、最容易被刀具顶走的位置，
  而中段两侧都有未切材料压着。
  次效果：斜坡段也不落在角点上。
- **`Horizontal` 与 `CW` 共同决定起点落在哪个角**，二者来源在 `OpsPara.vb:105-107`：

```vb
cutToolPath.CW = cutToolPath.ToolPath.CW = 1                                    ' 刀路旋向
cutToolPath.Horizontal = (MaxX-MinX) >= (MaxY-MinY)                             ' 包围盒宽 ≥ 高
```

- **为什么必须区分 `CW`（旋向）**：起点选在某个角之后，轮廓是「从哪条边走进这个点」的 ——
  这正是旋向决定的。也就是说 `CutDirect × Horizontal × CW` 一起确定的，
  是**哪一条边承担了归零的那一刀**。这是 15 路分支真正在做的事。
- `CutDirect = 0 / 15`（四周都没有/都有邻件，退化情况）时不做方向区分，
  只按横竖+旋向取固定角 —— 是兜底，不是优化。

### 2.3 与你们 `modRamp.bas` 的「选下刀边」对比

你们没有用位掩码，而是用几何启发式（`modRamp.bas:279-342` `SetGeoStartToSheetSide`）：

```
1. 找出距【排版板中心】最近的角（L1 距离最小）
2. 再沿该角所在边整边跳一次，落到【相邻边的中点】
3. toolGeo.SetStartPoint 到该点
```

配套还有一个「远的先切」排序（`:137` 倒序 + `:128` 计算 `|Δx|+|Δy|` 到板中心距离）。

| 维度 | 小条先切 | modRamp.bas |
|---|---|---|
| 输入 | 4 方向碰撞位掩码 + 旋向 | 排版板中心 + 包围盒 |
| 落点 | 角点内退 BackDist（40mm） | 相邻边中点 |
| 与邻件关系 | **显式建模**（真几何求交） | 不建模，只朝向板心 |
| 斜坡长度 | 固定 20mm | 由角度反算，且受边长 0.8 截断 |
| 工序 S/F | 硬编码覆盖 | 读原工序，读不到才回落 |
| 幂等 | 无（重复跑会重复切） | `CCC_RampDone` 属性标记 |
| 副作用 | 重建刀路，丢原工序关联 | 同样重建刀路 |

> ⚠️ `SetGeoStartToSheetSide` 的「整边跳一次」在数学上会跳出包围盒
> （例：100×600 的竖条，先选到 (100,300)，`edgeLen=600` 一跳得到 (50,-300)）——
> 依赖 `Path.SetStartPoint` 自身把点吸附到最近的路径点上才不至于出事。
> 目前能工作，但属于「靠下游兜住」的写法，建议改成直接算边的中点。

---

## 3. 切割顺序：为什么「小条先切」这个思路对真空是对的

`DrwNest.OptimizeCutPaths()`（`:202-251`）的主循环：

```vb
For j = 0 To count - 1
    SetCutPriority(toolPaths)              ' ① 重算四方向权重（对剩余集合）
    SortToolPathByAxis(toolPaths, IsNestH) ' ② 按 MaxX/MaxY 升序（仅当并列时的次级键）
    SortToolPathByCutSpecial(toolPaths)    ' ③ 小条置前（DepthCutSpecial 降序）
    SortToolPathByPriority(toolPaths)      ' ④ 权重降序 ← 最后执行，成为主排序键
    取 toolPaths(0) → 小条入 list / 大板入 list2；RemoveAt(0)
Next
' 先全部处理 list（小条，带斜坡），再处理 list2（大板）
```

小条判定（`:209-213`）：

```vb
If (Horizontal And 高 ≤ SmallPanel) Or (Not Horizontal And 宽 ≤ SmallPanel) Then DepthCutSpecial = 1
' SmallPanel = SmallWidth，默认 260mm
```

大板用 `RoughFinishBigPanel`：**没有斜坡**，且起点被统一设到排版图的 `(minX, minY)` 角。

**这个顺序在真空意义上是对的**，理由是泄漏路径而不是件自身面积：

- 排版图刚上料时，只有外轮廓，**台面密封完好**，真空度最高。
- 先切大板 → 板上开出大片通孔 → 空气从这些大孔直接漏向真空腔 →
  剩下的小条这时才切，**吸力已经掉下去了**，而小条恰恰是最需要吸力的一类。
- 反过来先切小条：小条被切开时四周全是整料，气流路径最短、密封最好；
  之后切大板时大板面积大、质量大，本来就不靠真空也稳。

即 `小条先切` 的赌注是：**把最脆弱、最需要吸力的那一刀，安排在密封最好的时刻**。
这个赌注是合理的，但它同时引入了一个代价 —— 小条一旦切开就**立即变成自由件**，
要靠真空独力按住，直到整张板加工完毕。

---

## 4. 真空吸不住小板件的真正机理

### 4.1 力估算（说明为什么「面积小」不是主因）

设真空表压 -0.06 MPa（≈ -0.6 bar），单位面积吸力 ≈ 0.06 N/mm²：

| 件尺寸 | 面积 | 满真空吸力 | 切离后（漏气只剩 20%） |
|---|---|---|---|
| 1000×600 大板 | 600 000 mm² | ≈ 3600 kgf | ≈ 720 kgf |
| 200×100 小条 | 20 000 mm² | ≈ 122 kgf | **≈ 24 kgf** |
| 100×60 小件 | 6 000 mm² | ≈ 37 kgf | **≈ 7 kgf** |

而 6mm 刀具在 MDF 上以 9000mm/min 切削时，侧向切削力量级在 5~30 kgf。
**对比可见：满真空时余量很大，漏气后立刻进入同一量级 → 件开始移动。**

> 结论：**「吸不住」的本质是漏气，不是面积。** 面积只决定了同样的漏气比例下你损失多少。

### 4.2 漏气的四个来源（按常见度）

1. **被切离的件自身周围 6mm 缝**：件一旦自由，四周就是通的。
2. **未覆盖的台面**：排版没占满板面时，空区的槽/孔直接漏气 → 必须**关闭空区阀门**。
3. **垫板（牺牲板/MDF）**：切痕越切越深、板面不平 → 垫板本身成为泄漏通道。需定期铣平/更换。
4. **管接头、软管、滤芯、密封条老化**：整机真空度掉。

### 4.3 判据（先分清是哪一类，再动手）

| 现象 | 指向 |
|---|---|
| 只有小件松动，大板稳定 | 件被切离**之后**失稳 → 走 5.1 / 5.4 / 5.5 |
| 小件在「入刀那一下」就被顶偏 | 斜坡段自身负载过大（坡太陡）→ 走 5.2 |
| **切离的那一瞬间**被推走 | 先确认归零特性真的生效：`SloopDist > 0` 且该小条走的是 `RoughFinish`（斜坡分支）**而不是**大板通道 `RoughFinishBigPanel`（无斜坡）。两者都在，则此因基本可排除 |
| 同一位置换一张排版就不松 | 排板把件放到了真空区边缘/密封条上 → 走 5.4 |
| 整张板都不稳、连大板也动 | 真空系统/垫板/阀门 → 走 5.5 |
| 跑「小条先切」之前正常、之后开始松 | 它覆盖了 S/F（1.5b）→ 先比对 NC 里的 F/S |

---

## 5. 解决方案（按性价比排序）

### 5.1 【最高】不要让件在主切削时切离 —— 留皮 / 微连接

> **与 1.3 的关系**：归零特性已经保证了「**切离那一瞬间**」是安全的，
> 所以留皮/微连接要解决的不是那一刻，而是**切离之后剩下的整段时间** ——
> 件已经自由，还要等后面所有刀路跑完（振动、切屑、相邻刀具经过）。
> 两者互补，不重复。

这是唯一能同时消除「件长期自由」和「漏气」两件事的手段。

- **留皮（两次走刀）**：第一趟全板所有件切到 `-(料厚 - 0.5~1.0mm)`，
  此时**没有任何一件被切离**，整板密封完整；第二趟再切透——第二趟只切 0.5~1.0mm，
  侧向力和振动都极小，件在这个时候才自由，但刀具马上离开。
- **原生参数入口（查 ACAMAPI 文档确认，见第 9 节）**：
  `NumberOfCuts`（「Number of Z levels」= Z 层数）+ `DepthsOfCutSpecified` = True
  + `ThicknessFirstCut` / `ThicknessLastCut`（「Z Thickness of first/last cut」）
  → 这就是**原生两趟分层**，不需要改刀路。
  > ⚠️ **更正**：本文早期版本曾建议用 `stock_z` → `StockZ` 做留皮，**这是错的**。
  > 文档原文：`StockZ` = 「Z Stock for **Z-Contour roughing**」—— 它是 Z 向轮廓
  > 粗加工的留量，不是通切时的留皮厚度。（`StockXY` 同理。）
- **微连接（tabs）**：按件周长均布 2~4 个 3~6mm 未切段，最后人工/程序掰断。
  对特别小的件比留皮更可靠（留皮的小件在第二趟仍可能被翻起）。
  > 根目录 `fix_ramp.py` 里已经有一版**未被采用的微连接草案**（见第 7 节）：
  > 把斜坡最后 10% 抬 0.5mm、把轮廓最后一个元素抬 0.5mm。
  > 思路对，但「只抬最后一个元素」等于只在一条边上留皮，位置随机且只有一个点，
  > 正确做法是按周长等分 2~4 个连接点。

### 5.2 【高】把斜下刀做成真斜下刀（但别弄丢归零特性）

> **改斜坡时唯一不能动的锚点**：斜坡必须**锚定在轮廓末端**（起点前 `sloopDist` 处开始、
> 在闭合点到达满深度）。只要锚点不变，1.3 的「末端归零」就自动保留；
> 一旦把斜坡改成「从起点往前若干毫米」（即以起点为斜坡**起始**而非**终点**），
> 负载会变成从零渐变到满，**切离反而落在满负载上** —— 比不做斜坡更糟。
> 你们 `modRamp.bas:199` 的 `rampStartDist = geoLen - sloopDist` 锚点与小条先切一致，
> 所以**归零特性在你们的实现里同样存在**，改角度不会破坏它。

1. **角度制，别用固定距离**：目标 5°~15°；18mm 料 10° → 斜坡 102mm。
   （只影响斜坡段自身负载，不影响末端归零。）
2. 斜坡长度受边长限制 → 短边退化为**往复斜坡（zig-zag）或螺旋下刀**。
   `create_mill_data` 里已经有 **`auto_z_ramp_angle` → `AutoZRampAngle`** 和
   **`helical_interpolation` → `HelicalInterpolation`** —— 优先用 **AlphaCAM 原生参数**，
   而不是像 `modRamp.bas` 那样删掉原刀路再手工重建（重建会丢工序/S/F 关联，
   且排版一变就得重跑）。⚠️ 换用原生参数时，务必确认它也是**末端锚定**的斜向入刀，
   否则会丢掉归零特性。
   > **文档已确认（见第 9 节）**：`AutoZ` = 「True for automatic multi-level cuts」，
   > `AutoZRampAngle` = 「Ramp angle for auto-z cuts」，`AutoZMaxDepthOfCut` /
   > `AutoZMinDepthOfCut` / `AutoZStockZ` 配套；另有 `LoopRadius` =
   > 「Radius for loops in RoughFinish」做环绕入刀。
   > 注意原生斜坡**挂在 auto-z（多分层）模式上** —— 所以单趟通切到底的工序里
   > 未必能直接用它，这一点需实机确认（第 9.2 节）。
3. **消除所有直插点**：`小条先切` 在半深/60% 首刀和「大板通道」里都是直插
   （`CutToolPath.vb:285/333/390/402`）。这些点必须一并改成斜坡或螺旋。
4. **大板也要斜坡**：`RoughFinishBigPanel` 完全没有斜坡，起点还被统一设到图角。
   薄板大件被直插顶起来同样会移位。
5. **下刀进给单独降**：`DownFeed` 从 2000 降到 800~1200，**且不要硬编码**，
   以小件区域为单位覆盖。

### 5.3 【高】下刀点选在「废料侧」而不是「零件角上」

现有两套实现都是在**零件轮廓上**找点。对最危险的小件，更稳的做法是：

1. 在排版废料区（条料与条料之间的空档）设一个**引刀点**；
2. 从废料区斜向下刀到满深度；
3. 再**引刀切入**轮廓起点。

这样入刀与斜坡都发生在**废料**上，零件轮廓从一开始就是满深度稳定切削，
且刀具第一次接触零件边缘时已经建立了切削状态，没有 Z 向冲击。
代价是每个件多一小段空行程 —— 对小件完全值得。

### 5.4 【中】排板与真空分区

- 小件尽量排在**真空区中心**，不要跨密封条、不要压在真空区边界。
- 排版未覆盖的区域**关闭对应分区阀门**（这是不花钱、见效最快的一条）。
- 板面利用率低时，用废板/贴纸**补盖空区**。

### 5.5 【中】机器与耗材

- 垫板定期**铣平**（1~2 周），切痕深了就换；垫板不平是头号隐形杀手。
- 检查整机真空度（-0.6 bar 以下先修泵/管路/滤芯，再谈算法）。
- 刀具：小件区域改用**下旋刀/压缩刀**（切削力把板压向台面而不是掀起来）。
  钝刀的影响比参数更大。
- 对小件区域单独降速：`CutFeed` 降 30~50%，比全局降速划算。

### 5.6 【中】按件尺寸反比调参（可在你们 VBA 里实现）

```
对每个件：吸力 ∝ 面积，切削力 ∝ 周长（近似）
→ F 应按 (件周长 / 件面积) 的比例反向缩放
即：越小的件，进给越低、下刀越慢、斜坡越长
```

这是一个很自然的规则，`小条先切` 和 `modRamp.bas` 都只做了「小件走斜坡」这一档，
没有做**连续**的参数缩放。

---

## 6. 建议的实施顺序（最小改动 → 最大收益）

| 步骤 | 动作 | 落点 | 预期 |
|---|---|---|---|
| 1 | 关闭空区真空阀、检查整机真空度、铣平垫板 | 机器 | 立即，可能直接解决问题 |
| 2 | 比对 NC：跑「小条先切」前后的 F/S 是否被改成 9000/2000 | `output_nc` 比对 | 排除 1.5b 的硬编码干扰 |
| 3 | `modRamp.bas` 的角度参数从默认调到 10°~15°，并确认对小条生效 | `CCC功能/modRamp.bas` | 消除入刀冲击 |
| 4 | 试「留皮两趟走刀」（第二趟切透 0.8mm） | 加工参数/NC 流程 | **治本** |
| 5 | 小件区域降 F、改用下旋刀 | 工艺 | 显著降低侧向力 |
| 6 | 实现「废料侧引刀 + 斜坡」 | 新插件/脚本 | 位 3 的加强版 |

---

## 7. 已核实：`fix_ramp.py` 是危险遗留（附一版微连接草案）

> 本节原列为「待确认」，现已用脚本核实。

`fix_ramp.py`（**已被 git 跟踪**，在仓库根目录）的行为是：

```python
src = 'modRamp_old.bas'                 # 读根目录的旧基线
dst = 'CCC功能/modRamp.bas'             # 写【当前在用】的插件
content = content.replace(old_sub, new_sub)   # 再打 3 个补丁
open(dst, 'w', encoding='gbk').write(content) # 直接覆盖
```

**实测三者关系**（`CCC功能/modRamp.bas` 342 行）：

| 特征 | `modRamp_old.bas` | `fix_ramp.py` 的替换文本 | `CCC功能/modRamp.bas`（实际在用） |
|---|---|---|---|
| `'左边中点` 注释 | 有 | 有（作为待替换文本） | **无** |
| `朝排版中心方向偏移整条边长`（hop-by-edge） | 无 | 无 | **有** |
| Fix1 `uniOff` 唯一偏移 | — | 有 | **无** |
| Fix2 `ratio > 0.9` 抬 0.5mm | — | 有 | **无** |
| Fix3 末元素 `z = finalDepth + 0.5` | — | 有 | **无** |

结论：`CCC功能/modRamp.bas` 是 modRamp_old.bas 之后的**第三代重写**
（引入 `rampSteps`、hop-by-edge 起点选择），`fix_ramp.py` 的三个补丁**从未生效**。

### ⚠️ 风险

`fix_ramp.py` 若再被执行一次，会用 **modRamp_old.bas + 3 个补丁**覆盖当前的第三代
`CCC功能/modRamp.bas`，**丢掉 hop-by-edge 起点选择逻辑** —— 一次误跑就回退两代。
建议二选一：

1. **删掉** `fix_ramp.py` 与 `modRamp_old.bas`（按仓库 `.gitignore` 自己的约定，
   一次性补丁应当带 `_` 前缀或放 `tmp/`，它们现在既在根目录又被跟踪）；
2. 或至少改名加 `_` 前缀 / 移入 `tmp/`，让它不再像一件可执行工具。

### 可回收：微连接草案

`fix_ramp.py` 的 Fix 2 / Fix 3 是**目前仓库里唯一一版微连接实现思路**，
虽未生效但值得回收：

```vb
' Fix 2：斜坡最后 10% 抬 0.5mm
Dim ratio As Double: ratio = actDist / sloopDist
Dim z As Double: z = -actualDepthAbs * ratio
If ratio > 0.9 Then z = z + 0.5 * ((ratio - 0.9) / 0.1)

' Fix 3：轮廓最后一个元素整体抬 0.5mm
If ei = elems2.Count Then z = finalDepth + 0.5
```

问题：连接点固定在**最后一个元素**（位置由刀路方向决定，不可控），且只有一个。
正确做法：按件周长等分 2~4 个连接段，每段 3~6mm 未切透。

---

## 8. 仍需实机确认（不要把推测当结论）

- `CutDirect` 位掩码的**极性**（第 2.1 节末尾）—— 反编译 GoTo 面条导致两种读法。
  最省的验证办法：拿一张已知排版在 `小条先切` 里跑一遍，用本项目 MCP 的
  `get_path_info` 读回刀路起点坐标反推。
- ~~`MillData.StockZ`（留料厚度）与 `RoughFinish()` 的交互~~ —— **已由文档关闭**：
  `StockZ` 是「Z Stock for Z-Contour roughing」，与通切留皮无关（见第 9 节）。
- **新增待确认**：`AutoZ` + `AutoZRampAngle`（原生斜坡）能否用在**单趟通切到底**的
  轮廓工序上（它挂在「automatic multi-level cuts」模式下）。若能，则不必手工重建刀路。
- **新增待确认**：`NumberOfCuts` / `DepthsOfCutSpecified` + `ThicknessFirstCut` /
  `ThicknessLastCut` 做**两趟留皮**时，两趟是否落在同一工序内、是否破坏加工道次关联。

---

## 9. 原生参数能力（ACAMAPI 文档实证）

文档源：`<AlphaCAM 安装目录>\tempacamapi\`（ACAMAPI 的 862 个 .htm 页面，已解压）。
以下均为**文档原文**，不是推测。

### 9.1 可用的原生能力

| 能力 | 参数 | 文档原文 | 用途 |
|---|---|---|---|
| **原生斜坡下刀** | `AutoZ` | "True for automatic multi-level cuts" | 开启分层 |
| | `AutoZRampAngle` | "Ramp angle for auto-z cuts" | **斜坡角（我们要的那个）** |
| | `AutoZMaxDepthOfCut` / `AutoZMinDepthOfCut` | "Maximum/Minimum depth of cut for auto-z cuts" | 分层厚度范围 |
| | `AutoZStockZ` | "Z stock for auto-z cuts" | 分层留量 |
| **原生两趟分层** | `NumberOfCuts` | "Number of Z levels" | Z 层数 |
| | `DepthsOfCutSpecified` | "True for specified depths of cut, also set ThicknessFirstCut and ThicknessLastCut" | 启用指定层深 |
| | `ThicknessFirstCut` / `ThicknessLastCut` | "Z Thickness of first/last cut" | **首/末层厚度 → 末层可做薄切** |
| **环绕入刀** | `LoopRadius` | "Radius for loops in RoughFinish" | 用环代替直插 |
| 引刀对象 | `App.CreateLeadData` / `CreateLeadData3D` | "Lead-In/Out data for use with a Machining Style or MillData" | 引刀数据 |
| 曲面引刀斜坡 | `SurfaceMCLeadRampAngle` / `SurfaceMCLeadRampLength` / `SurfaceMCLeadAndLinks` | "Ramp angle/length for lead…in surface machining" | 仅曲面加工 |
| **预钻孔入刀** | 官方示例注释 | "Create and drill holes that can be used to plunge into" / "so the Pre-Drilled-Holes option can be used" | 钻预孔后扎进孔里，彻底避开板面直插 |

> ⚠️ **`StockZ` 不是留皮参数**：原文 "Z Stock for **Z-Contour roughing**"；
> `StockXY` 同理。本文早期版本据此给出的建议是错的，已在 §5.1 更正。

### 9.2 对本方案的影响

1. **阶段 1（两趟留皮）不必自己搓刀路**：`NumberOfCuts=2` + `DepthsOfCutSpecified=True`
   + `ThicknessLastCut=0.8` + `FinalDepth=-(料厚+0.3)` 就能表达「先切到料厚-0.8、末层切透」。
   这条路**不动原刀路、不破坏加工道次**，优于 modRamp 那种删了重建的做法。
2. **原生斜坡挂在 auto-z（分层）模式下** —— 这是它相对手工斜坡的唯一限制：
   单趟到底的通切工序里能不能直接用，需要实机确认（第 8 节）。
3. `LoopRadius` 提供第三种入刀方式（环入），可与斜坡互补；
   短边放不下斜坡时可用环。

### 9.3 顺带确认的两个 API 语义（同样来自文档）

- **`Path.SetStartPoint(X, Y)`**：原文只有一句 —— "Set the start point for the path,
  **if path is closed**"，参数是"新起点的局部坐标"，**没有任何"自动吸附到路径上"的说明**。
  ⇒ 传入不在路径上的点属未定义行为。这**证实**了 `modRamp` v1.x 那个
  "沿边整边跳一次、跳出包围盒"的写法是错的，v2.0 改为直接算边中点是必需的修正。
  官方示例同样只传路径上的点：`Geo.SetStartPoint 50, 100`（0..100 矩形的上边中点）。
- **`MillManualToolPath.Add3DLine(X, Y, Z)`**：原文 "Add a 3D **feed** line, start point
  is end point of last line or arc, or point passed to `MillData.ManualToolPath` if this
  is the first line or arc" ⇒ Z 是**终点 Z**；首段的起点来自 `ManualToolPath(x,y,z)`。
  这证实了手工重建斜坡的写法（以及"从 Z=0 起、不做纯 Z 直插"的做法）是对的。
- **`Element` 的 Z 读取**：`StartZL`/`EndZL` = "Start/End Z **Local** coordinate"，
  `StartZG`/`EndZG` = Global ⇒ 逐元素校核斜坡与连接点的 Z 剖面是可行的（测试即用此法）。
